'use strict';

//Scene service used for managing scene
angular.module('core').service('Scene', ['$window',

	function($window) {
		// Initialize scene
		this.initializeScene = function () {
		};
	}
]);